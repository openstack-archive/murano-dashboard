---
fixes:
  - Fixed password-checking by adding 'password' to the names of all password
    fields. Previously password fields with the names not ending with
    'password' were not validated by javascript.
